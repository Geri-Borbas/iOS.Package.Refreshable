import XCTest
@testable import SwiftUI_Pull_to_Refresh

final class SwiftUI_Pull_to_RefreshTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUI_Pull_to_Refresh().text, "Hello, World!")
    }
}
